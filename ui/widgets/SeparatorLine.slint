import { VerticalBox } from "std-widgets.slint";
export component SeparatorLine {
    VerticalBox {
        spacing: 0px;

        line-rectangle := Rectangle {
            background: #a0a0a0;
            height: 1px;
        }

        line-rectangle2 := Rectangle {
            background: #ffffff;
            height: 1px;
        }
    }
}
